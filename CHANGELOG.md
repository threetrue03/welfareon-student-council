
# 변경 이력

## v1.0.18 - 2026-08-03

### 물품 기록 및 통계
- 소모품 지급 시 학생, 물품, 지급 수량, 처리 근무자, 메모를 별도 지급 기록으로 보존하도록 추가했습니다.
- 물품 기록 메뉴에 `소모품 기록`과 `통계` 화면을 추가했습니다.
- 요일별·시간대별 대여/반납 횟수, 비품별 대여 순위, 소모품별 지급 수량, 학생별 연체 순위를 제공합니다.
- 통계 기간을 최근 1주, 최근 1개월, 1학기, 여름방학, 2학기, 최근 1년, 전체 누적으로 선택할 수 있습니다.
- 기준 연도의 겨울방학을 상반기(전년도 12월~해당 연도 2월)와 하반기(해당 연도 12월~다음 연도 2월)로 구분했습니다.
- 시작일과 종료일을 직접 선택하는 사용자 지정 통계 기간을 추가했습니다.
- 반납 완료 연체와 현재 미반납 연체를 모두 연체 순위에 포함합니다.
- 통계 화면을 모바일과 어두움 모드에서도 사용할 수 있도록 반응형 스타일을 적용했습니다.

### 구글 시트 동기화
- 구글 시트에 `대여 현황` 탭을 추가했습니다.
- 현재 대여 중인 학생의 이름, 학번, 물품, 물품번호, 반납예정일을 표시하고 반납 후 자동으로 목록에서 제외합니다.

### 관리자 권한 관리
- 관리자 메뉴에 `관리자 학번 관리` 화면을 추가했습니다.
- 등록된 활성 근무자 계정의 관리자 학번을 추가·수정·삭제할 수 있습니다.
- 관리자 학번 CSV와 계정 역할을 함께 갱신하고, 현재 로그인한 본인의 권한 변경 및 마지막 관리자 삭제를 차단합니다.

### 런처 및 데이터 안정성
- 프로그램 시작 전에 Django 마이그레이션을 자동 적용하도록 런처를 개선했습니다.
- 실제 DB 업데이트가 적용되면 완료 알림창을 표시합니다.
- 소모품 지급 기록과 연결된 물품 삭제를 차단하고, 학생 삭제 및 DB 가져오기 과정에서도 관련 기록을 함께 안전하게 처리합니다.
- 비품 상태 선택지와 대여/반납 근무자 필드 정의를 현재 모델에 맞게 마이그레이션으로 정리했습니다.
- 전체 Django 테스트 18개와 시스템 검사를 통과했습니다.

## v1.0.17

- 모든 DB 변동성 버튼에 버튼 내부 로딩 애니메이션과 중복 제출 방지를 추가했습니다.
- 물품 대여 다음/처리, 물품 반납 처리, 저장/수정/삭제/설정/백업 버튼의 대기 상태를 통일했습니다.
- 사용자 미니창에 어두움 모드 토글을 추가했습니다.
- 어두움 모드를 다크 그레이 기반으로 설계하고 카드, 테이블, 입력창, 모달, 토스트, 달력 화면을 보정했습니다.
- 물품 대여/반납 JS의 중복 변수 선언 문제를 정리했습니다.

## v1.0.16 - 2026-05-17

### 런처/WebView 종료 안정화
- 종료 확인창 본문을 `런처를 종료하시겠습니까?`만 표시하도록 정리했습니다.
- PID 파일이 누락되거나 꼬여도 `--webview` 프로세스를 탐색해 종료하도록 보강했습니다.
- 프로그램 종료 시 WebView 프로세스 트리와 서버 포트 점유 PID 정리를 강화했습니다.
- Python 3.12.x를 우선 사용하도록 런처와 빌드 스크립트를 고정했습니다.

### DB 백업/가져오기 보호
- 관리자 메뉴에 `DB 전체 백업` 화면을 추가했습니다.
- 물품/학생/근무자 DB 가져오기 전 자동 백업 ZIP을 생성하도록 추가했습니다.
- 근무자 DB 가져오기 시 현재 로그인 관리자 보호, 관리자 0명 저장 차단, 현재 관리자 비밀번호 보존을 적용했습니다.
- 근무자 삭제를 실제 삭제가 아니라 비활성화 처리로 변경했습니다.

### 데이터 무결성
- 학생 DB 가져오기에서 학생회비 납부 여부를 반영하도록 수정했습니다.
- 대여/반납 기록과 연결된 물품 삭제를 차단했습니다.
- 물품이 연결된 카테고리 삭제를 차단했습니다.

## v1.0.15 - 2026-05-14

### DB 가져오기 UX
- 물품/학생/근무자 DB 가져오기 업로드 버튼에 처리 중 로딩 스피너를 추가했습니다.
- 업로드 처리 중 버튼을 비활성화해 중복 클릭과 중복 제출을 방지했습니다.
- 학생 DB/근무자 DB 가져오기 화면에서도 확인 모달과 업로드 로딩 처리가 안정적으로 동작하도록 스크립트 로드를 보강했습니다.

### DB 잠금 안정화
- SQLite 연결 timeout을 늘려 일시적인 DB 잠금 상황에서 바로 실패하지 않도록 완화했습니다.
- 물품/학생/근무자 DB 가져오기 저장 작업을 명시적인 트랜잭션으로 감싸고, DB 잠금 발생 시 500 오류 대신 사용자 안내 메시지를 표시하도록 수정했습니다.
- 근무자 DB 가져오기 중 `database is locked`가 발생하던 상황을 완화했습니다.

### 배포본 정리
- 배포본 버전을 v1.0.15로 올렸습니다.
- 배포본에서 실제 구글 서비스 계정 JSON과 테스트 xlsx 파일을 제외했습니다.

## v1.0.14 - 2026-05-14

### exe WebView 실행 안정화
- `dist` 폴더에서 빌드된 exe를 직접 실행해도 프로젝트 루트를 올바르게 인식하도록 수정했습니다.
- PyInstaller 빌드 시 `webview`, `pythonnet`, `clr_loader` 의존성 포함을 강화했습니다.
- exe 모드에서 WebView 모듈 로드 실패 시 pip 설치를 시도하지 않고 명확한 오류를 남기도록 수정했습니다.
- WebView 실행 직후 종료 감시 시간을 늘려 내부 창 실행 실패를 더 안정적으로 감지합니다.

### 구글 시트 연동 UX
- `연결 테스트`, `오늘 데이터 동기화` 로딩 표시는 버튼 내부 애니메이션만 유지했습니다.
- 하단에 중복으로 뜨던 로딩 상태 박스를 삭제했습니다.

## v1.0.12 - 2026-05-14

### 구글 시트 연동 UX
- `연결 테스트` 실행 중 로딩 애니메이션과 작업 안내 문구를 표시하도록 개선했습니다.
- `오늘 데이터 동기화` 실행 중 로딩 애니메이션과 작업 안내 문구를 표시하도록 개선했습니다.
- 처리 중 버튼을 비활성화해 중복 클릭과 중복 요청을 방지했습니다.

# WelfareON 업데이트 기록

## v1.0.11 - 2026-05-14

### 런처 대시보드 정리
- 프로그램 상태 카드에서 의미가 불명확하게 보이던 빨간색 하단 바 문제를 제거했습니다.
- `브라우저로 열기` 버튼을 삭제했습니다.
- `주소창 없는 내부 창으로 실행됩니다.` 문구를 삭제했습니다.
- 시작/종료 버튼만 남겨 프로그램 실행 흐름을 단순화했습니다.

### WebView 실행 관리
- WebView 내부 창 PID를 `logs/webview.pid`에 저장합니다.
- 이미 WebView가 실행 중이면 새 창을 중복으로 띄우지 않고 기존 창 사용 안내만 표시합니다.
- 비정상 종료로 남은 PID는 자동 정리합니다.
- 프로그램 종료 시 WebView 내부 창과 서버 프로세스를 함께 종료하고 PID 파일을 정리합니다.

### 구글 시트 연동 반영
- 런처에서 저장한 구글 시트 설정을 웹사이트가 실제로 읽는 `app/auth/google_sheets.json`에도 저장합니다.
- 서비스 계정 JSON은 `app/credentials/service-account.json`에 복사하고, 웹사이트 설정에는 `credentials/service-account.json` 상대경로를 저장합니다.
- v1.0.10 이하에서 런처 설정에만 남은 구글 시트 정보를 웹사이트에서 fallback으로 읽을 수 있도록 보강했습니다.

### 유지 사항
- 통계 탭 삭제 상태를 유지했습니다.
- 로그인 하단 로딩 박스 삭제 상태를 유지했습니다.
- v1.0.10의 환경 설정 화면 레이아웃 개선 상태를 유지했습니다.

## v1.0.10 - 2026-05-14

### 런처 레이아웃
- 환경 설정 화면을 고정 좌표 방식에서 유연한 레이아웃 방식으로 재구성했습니다.
- 작업 출력 영역, 하단 버튼 영역, 푸터 영역이 서로 겹치거나 잘리지 않도록 수정했습니다.
- 진행률 표기를 `진행률 n%` 형식으로 고정했습니다.

### 프로그램 시작 UX
- 프로그램 시작 버튼 클릭 후 `시작 중...` 상태를 표시하고 중복 클릭을 방지합니다.
- 서버 상태 확인, 서버 시작, 내부 창 실행 단계를 상태 문구로 표시합니다.
- 서버가 이미 실행 중이면 새 서버를 띄우지 않고 기존 서버에 연결합니다.

### WebView 안정성
- 내부 WebView 실행 실패가 숨겨지지 않도록 `logs/webview.log`에 로그를 저장합니다.
- WebView 프로세스가 실행 직후 종료되면 실패로 감지합니다.
- 내부 창 실행 실패 시 기본 브라우저로 열 수 있는 대체 경로를 추가했습니다.
- 런처 대시보드에 `브라우저로 열기` 버튼을 추가했습니다.

### 빌드/배포
- PyInstaller 빌드 시 pywebview 관련 하위 모듈 포함 옵션을 보강했습니다.
- 캐시, 로그, 임시 DB, 빌드 산출물 제외 정책을 유지했습니다.

## v1.0.9 - 2026-05-14

### 런처 환경 설정 화면
- 환경 설정 완료 후 하단 버튼이 잘려 보이는 문제를 수정했습니다.
- 작업 출력 영역, 하단 액션 영역, 푸터 영역을 분리했습니다.
- 진행률 퍼센트 표시가 잘리지 않도록 조정했습니다.

### 출력/로그
- Python 실행 환경에 UTF-8 설정을 적용해 한글 로그 깨짐을 완화했습니다.
- 사용자 화면에는 요약 로그를 표시하고, Django 마이그레이션 원문은 로그 파일에만 남기도록 정리했습니다.

### 로그인 UX
- 로그인 화면의 하단 로딩 박스를 삭제했습니다.
- 로그인 버튼 내부의 `로그인 중...` 표시만 유지했습니다.

### 빌드/서버 안정성
- build_launcher.bat의 PowerShell 의존을 제거했습니다.
- 빌드 로그를 콘솔과 logs/build_launcher.log에 함께 남기도록 build_launcher.py를 정리했습니다.
- 서버 상태 확인용 /health/ 엔드포인트를 추가했습니다.
- 포트 충돌과 서버 시작 실패 처리를 강화했습니다.
- 관리자 비밀번호를 명령어 인자가 아닌 표준 입력으로 전달하도록 변경했습니다.

### 배포 정리
- 배포 ZIP에서 __pycache__, .pyc, 로그, 임시 DB, 빌드 산출물을 제외했습니다.
- v1.0.7의 통계 탭 삭제 상태를 유지했습니다.

## v1.0.8 - 2026-05-14

### 빌드 파일 수정
- `build_launcher.bat` 실행 시 PowerShell에서 `AmpersandNotAllowed` 오류가 발생하던 문제를 수정했습니다.
- 배치파일 안에서 PowerShell 리디렉션을 `2>&1` 형태로 정상 전달하도록 정리했습니다.
- 빌드 로그를 콘솔과 `logs/build_launcher.log`에 동시에 출력하는 흐름은 유지했습니다.
- v1.0.7에서 반영한 통계 탭 전면 삭제 사항은 유지했습니다.

## v1.0.7 - 2026-05-14

### 통계 기능 제거
- 관리자 전용 `통계` 탭을 전면 삭제했습니다.
- 사이드바에서 `통계` 메뉴를 제거했습니다.
- 통계 페이지 URL, 뷰 함수, 템플릿, 전용 CSS를 제거했습니다.
- v1.0.6의 런처 환경 설정 화면 개선 사항은 유지했습니다.

## v1.0.6 - 2026-05-14

### 런처 환경 설정 화면
- 작업 출력 영역 높이를 확대하고 세로 스크롤바를 추가했습니다.
- 로그가 추가될 때 최신 출력 위치로 자동 이동하도록 유지했습니다.
- 진행률 숫자가 퍼센트 형식으로 명확히 보이도록 유지했습니다.
- 세부 작업 문구가 어색하게 잘리지 않도록 줄바꿈 폭과 영역 높이를 조정했습니다.
- 하단 버튼/푸터와 작업 출력 영역이 겹치지 않도록 런처 창 높이와 내부 배치를 조정했습니다.

### 빌드 파일
- build_launcher.bat 실행 시 빌드 로그가 콘솔에도 보이도록 개선했습니다.
- logs/build_launcher.log 저장은 유지했습니다.
- 혼동을 줄 수 있는 build_bauncher.bat 오타 대응 파일을 제거했습니다.

## v1.0.5 - 2026-05-14

### 런처 중복 실행 방지
- Windows Mutex 방식으로 런처 단일 실행 잠금을 추가했습니다.
- 이미 런처가 실행 중이면 새 런처 창을 띄우지 않고 안내창을 표시한 뒤 종료합니다.
- WebView 실행 모드는 중복 실행 잠금 대상에서 제외하여 프로그램 내부 창은 정상적으로 열리도록 처리했습니다.
- 기존 서버 실행 여부를 확인해 새 서버를 중복 실행하지 않는 흐름은 유지했습니다.

## v1.0.4 - 2026-05-14

### 런처/빌드 안정화
- `build_launcher.bat` 줄바꿈을 Windows CRLF 기준으로 정리해 `65001`, `UILD_LOG`, `rorlevel`처럼 명령이 깨지는 문제를 수정했습니다.
- 경로에 공백이 있어도 안전하게 실행되도록 `pushd`와 따옴표 처리 방식을 보강했습니다.
- `build_bauncher.bat` 오타 실행을 대비한 호환 배치 파일을 추가했습니다.

### 환경 설정 UX
- 환경 설정 화면의 왼쪽 진행 단계 박스를 제거했습니다.
- 현재 작업, 세부 작업, 진행률, 작업 출력을 한 화면에서 확인하도록 화면 구성을 변경했습니다.
- 프로그레스바를 왕복 애니메이션 방식에서 실제 퍼센트 기반 진행률로 변경했습니다.
- 가상환경 생성, 패키지 설치, DB migration, 관리자 계정 생성 출력이 작업 출력 영역에 표시되도록 보강했습니다.

### 조회 화면
- 아직 반납되지 않은 대여 기록의 반납 근무자가 `삭제됨`으로 표시되던 문제를 수정했습니다.
- 미반납 상태의 반납 근무자는 `-`로 표시하도록 변경했습니다.
- 학생으로 조회/물품으로 조회의 반납 예정일에 `D-day`, `D-n`, `D+n` 표기를 추가했습니다.

### 통계
- 관리자 전용 `통계` 탭을 추가했습니다.
- 오늘 대여/반납, 현재 대여 중, 현재 연체, 고장/분실 물품 요약 카드를 추가했습니다.
- 기간별 대여/반납 현황, 많이 대여된 물품 TOP 10, 카테고리별 대여 현황, 미반납/연체 목록, 근무자별 처리 요약을 추가했습니다.

## v1.0.3 - 2026-05-13

### 런처 안정화
- 프로그램 환경 설정 단계에 진행 프로그레스바를 추가했습니다.
- 환경 설정 완료 후 별도 자동 이동 없이 같은 화면에서 `다음` 버튼이 활성화되도록 변경했습니다.
- 최초 설정 시 기존 테스트 DB를 삭제하고 깨끗한 DB로 시작하도록 수정했습니다.
- 서버 중복 실행을 감지해 같은 DB를 사용하는 서버가 중복으로 뜨지 않도록 수정했습니다.
- 서버 PID 파일을 관리해 프로그램 종료 시 기존 서버를 정리하도록 보강했습니다.
- `build_launcher.bat` 실패 시 바로 꺼지지 않고 `logs/build_launcher.log`에 기록을 남기도록 수정했습니다.

### WebView/URL 처리
- WebView 내부에서 Shift+Enter, 새 창 열기, 외부 URL 이탈을 차단했습니다.
- 링크/폼의 target 속성을 자동 제거하도록 보강했습니다.

### 로그인/표시
- 로그인 로딩 표시가 버튼 클릭 전부터 보이는 문제를 수정했습니다.
- 학과(부) 명 뒤에 `학생회`가 중복 표시되는 문제를 수정했습니다.

## v1.0.2 - 2026-05-13

### 런처
- 종료 확인창에서 확인을 눌렀을 때 런처가 종료되지 않는 문제를 수정했습니다.
- 종료 시 실행 중인 프로그램을 정리하고 런처 프로세스를 확실히 종료하도록 보강했습니다.

### 물품 관리
- 물품별 위치를 입력하고 DB에 저장할 수 있는 `물품 위치` 필드를 추가했습니다.
- 물품 관리 표를 `물품명 | 물품 위치 | 유형 | 카테고리 | 수량/상태 | 관리` 구조로 변경했습니다.
- 물품 추가/수정 모달에 물품 위치 입력칸을 추가했습니다.
- 물품 검색에서 물품 위치도 함께 검색되도록 개선했습니다.

### 대여/반납
- 물품 대여/지급 화면의 번호 선택 영역에 물품 위치를 표시하도록 변경했습니다.
- 물품 반납 화면의 반납 번호 선택 영역에 물품 위치를 표시하도록 변경했습니다.

### DB 가져오기/배포
- 물품 DB 가져오기 양식을 `물품명 | 물품 위치 | 카테고리 | 물품 특성 | 전체 수량 | 현재수량` 구조로 변경했습니다.
- 물품 DB 양식 xlsx 파일을 새 구조에 맞게 갱신했습니다.
- 구글 시트 재고 현황에 물품 위치 열을 추가했습니다.

### 사이드바
- 사이드바를 닫았을 때 사용자 아이콘이 표시되지 않도록 수정했습니다.

## v1.0.1 - 2026-05-13

### 런처
- 관리자 학번 입력을 숫자만 허용하도록 변경했습니다.
- 현재 작업/로그 영역을 읽기 전용으로 변경했습니다.
- 프로그램 환경 설정 단계에서 현재 세부 작업을 표시하도록 개선했습니다.
- 환경 설정 완료 후 구글 시트 단계로 자동 이동하지 않고, 사용자가 `다음`을 눌러 이동하도록 변경했습니다.
- 로그 파일 보기 버튼을 추가했습니다.
- 환경 설정 오류 발생 시 사용자에게 알리고 초기화 실패 상태를 저장하지 않도록 수정했습니다.
- 창 닫기 버튼 클릭 시 확인 경고창을 표시하도록 변경했습니다.
- `단체명` 표현을 `학과(부) 명`으로 변경했습니다.
- 입력받은 학과(부) 명을 `<학과(부)명> 학생회` 형식으로 프로그램에 반영합니다.
- 구글 시트 연동을 건너뛰면 스프레드시트 ID와 서비스 계정 JSON 경로를 저장하지 않도록 수정했습니다.

### 웹 프로그램
- 로그인 처리 중 로딩 상태를 표시하도록 개선했습니다.
- 근무 종료 처리 중 로딩 상태를 표시하도록 개선했습니다.
- 근무 종료 버튼의 장식 이모티콘을 제거했습니다.
- 사용자 메뉴에 비밀번호 변경 기능을 추가했습니다.
- 비밀번호 변경 성공/실패 메시지를 토스트/메시지로 표시합니다.
- 로그인 화면과 사이드바 상단에 `<학과(부)명> 학생회`가 표시되도록 수정했습니다.

### 데이터/배포
- 학생 더미 데이터가 생성되지 않도록 초기 마이그레이션을 정리했습니다.
- 배포본에 실제 DB, 실제 관리자 CSV, 실제 구글 서비스 계정 JSON이 포함되지 않도록 정리했습니다.
- 업데이트 기록 문서를 추가했습니다.

## v1.0.0 - 2026-05-13

### 최초 배포
- 복지온 런처와 Django 기반 복지 관리 프로그램 배포본을 구성했습니다.
- 학생/물품/근무자 관리, 대여/반납, 근무 기록, 구글 시트 연동 기능을 포함했습니다.
